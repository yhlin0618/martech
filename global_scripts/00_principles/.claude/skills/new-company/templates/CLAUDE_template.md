# CLAUDE.md - {COMPANY} Project

> Claude Code 在此專案工作時的指南

## 🚨 首次設定檢查清單

**使用者必須先完成以下設定，否則專案無法運作：**

### ⚠️ 必須手動設定

- [ ] **Rawdata 資料夾** - `data/local_data/rawdata_{COMPANY}/`
  ```bash
  # 檢查是否存在
  ls -la data/local_data/rawdata_{COMPANY}

  # 如果不存在，提醒使用者設定：
  # Option A: ln -s /dropbox/path data/local_data/rawdata_{COMPANY}
  # Option B: 用 Finder 移動 Dropbox 資料夾
  # Option C: mkdir -p data/local_data/rawdata_{COMPANY}
  ```

- [ ] **環境變數** - `.env` 檔案
  ```bash
  # 檢查是否存在
  ls -la .env

  # 如果不存在：
  cp .env.template .env
  chmod 644 .env
  # 提醒使用者填入實際憑證
  ```

- [ ] **啟用平台** - `app_config.yaml`
  ```yaml
  # 檢查 platform: 區塊是否有啟用項目
  # 檢查 pipeline.platforms: 對應項目是否 enabled: true
  ```

### 驗證設定

```bash
# 測試連線
Rscript scripts/global_scripts/98_test/test_connections.R

# 測試公司識別
Rscript -e 'source("scripts/global_scripts/04_utils/fn_resolve_script_path.R"); print(get_company_from_config())'
```

---

## 📋 原則系統

**在修改程式碼前，必須先查閱原則：**

```
scripts/global_scripts/00_principles/
├── INDEX.md              # 原則索引
├── QUICK_REFERENCE.md    # 快速查詢
└── llm/index.yaml        # AI 最佳化格式
```

### 常用原則

| 原則 | 說明 |
|------|------|
| **MP122** | Quad-Track Shared Symlink Architecture |
| **MP064** | ETL-Derivation Separation |
| **SO_P016** | Configuration Scope Hierarchy |
| **DM_R037** | Company-Specific ETL Naming (`___COMPANY` suffix) |

---

## 🔧 開發指令

### Pipeline 操作

```bash
cd scripts/update_scripts

make config-merge          # 合併配置
make run PLATFORM=cbz      # 執行指定平台
make run                   # 執行所有啟用平台
make status                # 檢查狀態
make vis                   # 視覺化 DAG
```

### Shared Git 操作

```bash
# 一般變更（直接 push，不需 PR）
cd shared/global_scripts
git add . && git commit -m "[TYPE] description" && git pull && git push

cd shared/update_scripts
git add . && git commit -m "[TYPE] description" && git pull && git push

# 有特定 Issue 需要正式審查時
cd shared/global_scripts
git checkout -b fix/issue-description
# ... 修改 ...
git add . && git commit -m "[FIX] description"
git push -u origin fix/issue-description
gh pr create --title "Fix: description" --body "Closes #123"
```

### 啟動應用

```bash
Rscript app.R              # 本地開發
```

---

## 📁 專案結構

```
{COMPANY}/
├── app.R                    # 進入點 - 讀取 config 執行 main_file
├── app_config.yaml          # Track 3: 專案配置
├── .env                     # 環境變數（勿提交）
├── .claude -> scripts/global_scripts/00_principles/.claude  # Claude Code 整合
│
├── data/
│   ├── local_data/
│   │   └── rawdata_{COMPANY}/  # ⚠️ 使用者手動設定
│   └── app_data/               # 應用資料
│
├── shared/
│   ├── global_scripts/      # Track 1: Framework (standalone git)
│   │   ├── 00_principles/   # 原則系統
│   │   │   └── .claude/     # Skills & Agents 定義
│   │   ├── 04_utils/        # 工具函數
│   │   ├── 10_rshinyapp_*/  # Shiny 元件
│   │   └── 22_initializations/
│   │
│   └── update_scripts/      # Track 2: ETL/DRV (standalone git)
│       ├── ETL/             # 資料擷取
│       │   ├── cbz/
│       │   ├── eby/
│       │   └── amz/
│       └── DRV/             # 資料衍生
│
└── scripts/
    ├── global_scripts -> ../../shared/global_scripts  # symlink
    └── update_scripts -> ../../shared/update_scripts  # symlink
```

> **Note**: `.claude` 是 symlink，指向 `scripts/global_scripts/00_principles/.claude`，讓 Claude Code 可以存取共用的 skills 和 agents。
> `scripts/global_scripts` 和 `scripts/update_scripts` 是 symlink，指向 `shared/` 中的 standalone git repos。

---

## ⚠️ 注意事項

1. **修改 global_scripts 前請三思** - 這是共用 repo，變更會影響所有公司
2. **公司專屬腳本** - 使用 `___COMPANY` 後綴（如 `cbz_ETL_sales_0IM___{COMPANY}.R`）
3. **敏感資料** - `.env`、`data/local_data/` 已在 .gitignore
4. **Rawdata** - 不要用程式移動，可能破壞 Dropbox 同步

---

## 🔗 快速連結

- **部署指南**: `scripts/global_scripts/23_deployment/05_docs/NEW_COMPANY_DEPLOYMENT.md`
- **原則索引**: `scripts/global_scripts/00_principles/INDEX.md`
- **Skill 來源**: `scripts/global_scripts/00_principles/.claude/skills/new-company/`

---

*Generated by `/new-company` skill on {DATE}*
