---
title: "DM_R001: Data Frame Creation Strategy Pattern"
subtitle: "Strategy Pattern implementation for consistent data frame creation, validation, and storage"
chapter: "CH02"
category: "rule"
number: "DM_R001"
date-created: "2025-04-04"
date-modified: "2025-08-17"
author: "Claude"
type: "rule"
law: "資料管理法施行細則 (Data Management Law Implementation Rules)"
article: "第1條"
implements:
  - "CH00.MP017: Separation of Concerns"
  - "CH00.MP018: Don't Repeat Yourself"
related_to:
  - "CH01.SO_R023: Object Naming Convention"
  - "CH01.SO_R028: Type-Token Distinction in Naming"
  - "CH02.DM_P001: Data Integrity"
format:
  html:
    toc: true
    toc-depth: 3
    code-fold: false
    code-tools: true
    number-sections: true
---

# 條文概述 {#overview}

**資料管理法施行細則第1條（資料框架創建策略模式）**

本條規定資料框架創建函數必須實作策略模式，以確保一致的表格創建、驗證和儲存，同時支援不同類型的資料框架、儲存選項和資料庫配置。

:::{.callout-important}
## 核心要求
資料框架創建函數必須實作策略模式，以確保表格創建、驗證和儲存的一致性，同時支援不同類型的資料框架、儲存選項和資料庫配置。
:::

# 第一項：設計模式選擇 {#design-pattern}

## 第一款：策略模式選擇理由 {#pattern-rationale}

策略模式被選為資料框架創建最適合的設計模式，因為：

1. **不同實作需求**：不同資料框架需要專門的創建邏輯（標準、索引、分區、時間序列）
2. **執行時期選擇**：確切的創建策略通常在執行時期決定
3. **關注點分離**：清晰地分離資料框架創建機制與業務邏輯
4. **可擴展性**：可以新增新策略而不修改現有程式碼
5. **配置驅動**：創建策略可以在配置中指定而非程式碼

# 第二項：實作指導原則 {#implementation}

## 第一款：策略註冊表 {#strategy-registry}

應維護資料框架創建策略的中央註冊表：

```r
# 資料框架創建策略的中央註冊表
df_creators <- list()

# 註冊函數
register_df_creator <- function(df_type, creator_fn) {
  df_creators[[df_type]] <- creator_fn
}

# 調度函數
create_or_replace_df <- function(object_name, data, df_type = "standard", ...) {
  # 取得正確的策略
  creator <- df_creators[[df_type]]
  
  if (is.null(creator)) {
    stop("No data frame creator registered for type: ", df_type)
  }
  
  # 執行策略
  return(creator(object_name, data, ...))
}
```

## 第二款：標準策略實作 {#standard-strategies}

### 標準資料框架

```r
# 註冊標準資料框架創建器
register_df_creator("standard", function(object_name, data) {
  # 驗證資料結構
  validate_df_structure(data, object_name)
  
  # 儲存資料框架（實作取決於儲存系統）
  if (is_db_storage_enabled()) {
    # 將物件名稱轉換為資料庫儲存的表格名稱
    table_name <- object_name_to_table_name(object_name)
    
    # 連接到資料庫
    con <- dbConnect(duckdb::duckdb(), get_db_path())
    on.exit(dbDisconnect(con), add = TRUE)
    
    # 創建或替換表格
    if (dbExistsTable(con, table_name)) {
      dbRemoveTable(con, table_name)
    }
    
    # 寫入表格
    dbWriteTable(con, table_name, data)
  }
  
  # 返回具有適當類別屬性的資料框架
  class(data) <- c(object_name, "data.frame")
  
  # 指派到全域環境
  assign(gsub("\\.", "_", object_name), data, envir = .GlobalEnv)
  
  return(data)
})
```

### 索引資料框架

```r
# 註冊索引資料框架創建器
register_df_creator("indexed", function(object_name, data, index_columns) {
  # 驗證資料結構
  validate_df_structure(data, object_name)
  
  # 驗證索引欄位
  if (!all(index_columns %in% names(data))) {
    missing <- setdiff(index_columns, names(data))
    stop("Index columns not found in data: ", paste(missing, collapse = ", "))
  }
  
  # 儲存資料框架
  if (is_db_storage_enabled()) {
    # 轉換名稱並連接資料庫
    table_name <- object_name_to_table_name(object_name)
    con <- dbConnect(duckdb::duckdb(), get_db_path())
    on.exit(dbDisconnect(con), add = TRUE)
    
    # 創建或替換表格
    if (dbExistsTable(con, table_name)) {
      dbRemoveTable(con, table_name)
    }
    
    # 寫入表格
    dbWriteTable(con, table_name, data)
    
    # 創建索引
    for (col in index_columns) {
      index_name <- paste0(table_name, "_", col, "_idx")
      dbExecute(con, sprintf(
        "CREATE INDEX %s ON %s(%s)",
        index_name, table_name, col
      ))
    }
  }
  
  # 添加索引元資料
  attr(data, "index_columns") <- index_columns
  
  # 設定類別
  class(data) <- c(object_name, "indexed_df", "data.frame")
  
  # 指派到全域環境
  assign(gsub("\\.", "_", object_name), data, envir = .GlobalEnv)
  
  return(data)
})
```

### 時間序列資料框架

```r
# 註冊時間序列資料框架創建器
register_df_creator("time_series", function(object_name, data, time_column = "date") {
  # 驗證資料結構
  validate_df_structure(data, object_name)
  
  # 驗證時間欄位
  if (!time_column %in% names(data)) {
    stop("Time column not found in data: ", time_column)
  }
  
  # 確保時間欄位格式正確
  if (is.character(data[[time_column]])) {
    data[[time_column]] <- as.Date(data[[time_column]])
  }
  
  # 儲存資料框架
  if (is_db_storage_enabled()) {
    # 資料庫操作（與上述類似）
    # ...
  }
  
  # 添加時間序列元資料
  attr(data, "time_column") <- time_column
  
  # 設定時間序列方法的類別
  class(data) <- c(object_name, "ts_df", "data.frame")
  
  # 指派到全域環境
  assign(gsub("\\.", "_", object_name), data, envir = .GlobalEnv)
  
  return(data)
})
```

# 第三項：輔助函數 {#helper-functions}

## 第一款：驗證函數 {#validation}

```r
# 驗證資料框架結構
validate_df_structure <- function(data, object_name) {
  if (!is.data.frame(data)) {
    stop("Data must be a data frame")
  }
  
  # 基於物件名稱的額外驗證
  # 例如，根據命名慣例檢查必要欄位
  
  return(TRUE)
}
```

## 第二款：配置函數 {#configuration}

```r
# 檢查是否啟用資料庫儲存
is_db_storage_enabled <- function() {
  # 從配置或環境讀取
  getOption("df_db_storage", TRUE)
}

# 取得資料庫路徑
get_db_path <- function() {
  # 從配置或環境讀取
  getOption("df_db_path", "app_data.duckdb")
}

# 將物件名稱轉換為表格名稱
object_name_to_table_name <- function(object_name) {
  # 擷取主要部分（三重底線之前）
  main_part <- strsplit(object_name, "___")[[1]][1]
  
  # 將點替換為底線
  table_name <- gsub("\\.", "_", main_part)
  
  return(table_name)
}
```

# 第四項：使用範例 {#usage-examples}

## 第一款：標準資料框架 {#standard-example}

```r
# 準備資料
customer_data <- read_csv("customer_data.csv")

# 創建標準資料框架
df.amazon.customer <- create_or_replace_df("df.amazon.customer", customer_data)

# 使用資料框架
head(df.amazon.customer)
```

## 第二款：索引資料框架 {#indexed-example}

```r
# 準備資料
product_data <- read_csv("product_data.csv")

# 創建索引資料框架
df.amazon.product <- create_or_replace_df(
  "df.amazon.product", 
  product_data, 
  "indexed",
  index_columns = c("product_id", "category")
)

# 使用具有索引知識的資料框架
products_in_category <- df.amazon.product[df.amazon.product$category == "Electronics", ]
```

## 第三款：時間序列資料框架 {#timeseries-example}

```r
# 準備資料
sales_data <- read_csv("sales_data.csv")

# 創建時間序列資料框架
df.amazon.sales_daily <- create_or_replace_df(
  "df.amazon.sales_daily", 
  sales_data, 
  "time_series",
  time_column = "sale_date"
)

# 使用時間序列方法
monthly_summary <- aggregate(df.amazon.sales_daily$revenue, 
                            by = list(month = format(df.amazon.sales_daily$sale_date, "%Y-%m")), 
                            FUN = sum)
```

## 第四款：自訂策略註冊 {#custom-strategy}

```r
# 註冊分區資料框架的自訂策略
register_df_creator("partitioned", function(object_name, data, partition_column) {
  # 驗證
  validate_df_structure(data, object_name)
  
  if (!partition_column %in% names(data)) {
    stop("Partition column not found in data: ", partition_column)
  }
  
  # 資料庫儲存實作
  if (is_db_storage_enabled()) {
    # 實作細節...
  }
  
  # 添加元資料
  attr(data, "partition_column") <- partition_column
  
  # 設定類別
  class(data) <- c(object_name, "partitioned_df", "data.frame")
  
  # 指派到全域環境
  assign(gsub("\\.", "_", object_name), data, envir = .GlobalEnv)
  
  return(data)
})

# 使用自訂策略
df.amazon.sales_by_region <- create_or_replace_df(
  "df.amazon.sales_by_region", 
  sales_data, 
  "partitioned",
  partition_column = "region"
)
```

# 第五項：效益 {#benefits}

## 第一款：主要效益 {#main-benefits}

:::{.callout-note}
## 策略模式效益
1. **一致性**：確保所有資料框架以一致的驗證和儲存創建
2. **可擴展性**：可以新增新的資料框架類型而不修改現有程式碼
3. **關注點分離**：將資料框架創建邏輯與業務邏輯分離
4. **集中配置**：儲存選項可以在一處更改
5. **類型安全**：為不同類型的資料框架強制執行特定結構
6. **元資料保留**：維護資料框架的重要元資料
7. **儲存彈性**：抽象化儲存細節，使更改儲存後端更容易
:::

# 實作檢查清單 {#checklist}

:::{.callout-note}
## 資料框架創建檢查清單

實作資料框架創建時，確認：

- [ ] 策略在中央註冊表中註冊
- [ ] 創建前驗證資料結構
- [ ] 物件名稱正確轉換為表格名稱
- [ ] 資料庫連接適當管理
- [ ] 索引為索引資料框架創建
- [ ] 時間欄位為時間序列資料正確格式化
- [ ] 元資料屬性已設定
- [ ] 類別屬性正確指派
- [ ] 資料指派到全域環境
- [ ] 錯誤處理已實作
:::

# 結論 {#conclusion}

資料框架創建策略模式規則建立了在精準行銷系統中創建和管理資料框架的靈活、可擴展方法。透過實作策略模式，本規則確保資料框架以一致的方式創建，具有適當的驗證、儲存和元資料指派。該模式允許輕鬆擴展以支援新類型的資料框架，同時保持與現有程式碼的向後相容性。

---

*依據：資料管理法施行細則第1條*  
*實作：[MP017 關注點分離](../../CH00_fundamental_principles/MP017_separation_of_concerns.qmd)、[MP018 不要重複自己](../../CH00_fundamental_principles/MP018_dont_repeat_yourself.qmd)*  
*相關：[DM_P001 資料完整性](../principles/DM_P001_data_integrity.qmd)*