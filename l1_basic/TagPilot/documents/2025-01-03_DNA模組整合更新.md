# DNA 模組整合更新記錄

**日期**: 2025-01-03  
**更新者**: Claude AI Assistant  
**版本**: v1.2.0

## 📋 更新摘要

將 TagPilot 的 DNA 分析模組從自定義實作改為使用標準化的 `fn_analysis_dna` 函數，與 TagPilot Pro (l2_pro) 版本保持一致。

## 🎯 更新目標

1. 統一 l1_basic 和 l2_pro 版本的 DNA 分析邏輯
2. 使用標準化的 `fn_analysis_dna` 函數進行客戶分析
3. 保持原有的九宮格視覺化呈現方式
4. 修正資料處理中的錯誤

## 📝 主要修改內容

### 1. 模組檔案修改
**檔案**: `/modules/module_dna_multi_basic.R`

#### 1.1 函數載入改進（第22-31行）
```r
# 修正前：載入但未驗證
if (file.exists("scripts/global_scripts/04_utils/fn_analysis_dna.R")) {
  source("scripts/global_scripts/04_utils/fn_left_join_remove_duplicate2.R")
source("scripts/global_scripts/04_utils/fn_fct_na_value_to_level.R")
source("scripts/global_scripts/04_utils/fn_analysis_dna.R")
}

# 修正後：載入並確認
if (file.exists("scripts/global_scripts/04_utils/fn_analysis_dna.R")) {
  source("scripts/global_scripts/04_utils/fn_left_join_remove_duplicate2.R")
  source("scripts/global_scripts/04_utils/fn_fct_na_value_to_level.R")
  source("scripts/global_scripts/04_utils/fn_analysis_dna.R")
  
  # 確保函數正確載入
  if (exists("analysis_dna")) {
    message("✓ DNA analysis function loaded successfully")
  }
}
```

#### 1.2 資料處理邏輯重構（第175-285行）

##### 修正前：
- 手動計算 M（總消費金額）和 F（購買頻率）
- 嘗試處理多個 Amazon 相關欄位（Item Tax, Shipping Price 等）
- 簡單的客戶分組計算

##### 修正後：
- 直接使用 `lineitem_price` 作為金額（參照 l2_pro 方法）
- 準備符合 `analysis_dna` 函數需求的資料格式
- 呼叫標準 DNA 分析函數

```r
# 新的資料準備流程
# 1. 按客戶按日期彙總
df_sales_by_customer_by_date <- data %>%
  mutate(date = as.Date(payment_time)) %>%
  group_by(customer_id, date) %>%
  summarise(
    sum_spent_by_date = sum(total_amount, na.rm = TRUE),
    count_transactions_by_date = n(),
    payment_time = min(payment_time, na.rm = TRUE),
    .groups = 'drop'
  )

# 2. 按客戶彙總並計算 RFM 指標
df_sales_by_customer <- data %>%
  group_by(customer_id) %>%
  summarise(
    total_spent = sum(total_amount, na.rm = TRUE),
    times = n(),
    first_purchase = min(payment_time, na.rm = TRUE),
    last_purchase = max(payment_time, na.rm = TRUE),
    .groups = 'drop'
  ) %>%
  mutate(
    # 計算 IPT (Inter-Purchase Time)
    ipt = pmax(as.numeric(difftime(last_purchase, first_purchase, units = "days")) / 
               pmax(times - 1, 1), 1),
    r_value = as.numeric(difftime(Sys.time(), last_purchase, units = "days")),
    f_value = times,
    m_value = total_spent / times,
    ni = times
  )

# 3. 呼叫 analysis_dna 函數
dna_results <- analysis_dna(
  df_sales_by_customer = df_sales_by_customer,
  df_sales_by_customer_by_date = df_sales_by_customer_by_date,
  skip_within_subject = TRUE,
  verbose = TRUE
)
```

#### 1.3 九宮格分割錯誤修正（第348-416行）

##### 問題：
- `cut.default: 'breaks' are not unique` 錯誤
- 當資料分佈不均時，分位數可能相同

##### 解決方案：
```r
# 處理分位數重複的情況
if (length(unique(m_quantiles)) < 2 || diff(m_quantiles) < 0.001) {
  # 使用三等分方法
  m_min <- min(df$M, na.rm = TRUE)
  m_max <- max(df$M, na.rm = TRUE)
  if (m_max - m_min < 0.001) {
    # 所有值相同，創建微小差異
    m_breaks <- c(m_min - 0.001, m_min, m_min + 0.001, m_max + 0.001)
  } else {
    # 使用三等分
    m_breaks <- c(m_min - 0.001, 
                 m_min + (m_max - m_min) * 0.33, 
                 m_min + (m_max - m_min) * 0.67, 
                 m_max + 0.001)
  }
}

# 確保 breaks 是唯一且遞增的
m_breaks <- sort(unique(m_breaks))
f_breaks <- sort(unique(f_breaks))

# 如果仍然沒有足夠的分割點，使用預設值
if (length(m_breaks) < 4) {
  m_breaks <- c(0, 100, 500, max(df$M, na.rm = TRUE) + 1)
}
```

#### 1.4 結果欄位處理改進（第254-278行）

##### 修正前：
```r
# 使用複雜的 ifelse 巢狀結構（導致錯誤）
M = ifelse("m_value" %in% names(.) && !is.na(m_value), m_value, 
          ifelse("total_spent" %in% names(.), total_spent, 0))
```

##### 修正後：
```r
# 使用簡單的條件判斷
if ("m_value" %in% names(customer_data)) {
  customer_data$M <- customer_data$m_value
} else if ("total_spent" %in% names(customer_data)) {
  customer_data$M <- customer_data$total_spent
} else {
  customer_data$M <- 0
}
```

## 🔄 資料流程

1. **資料接收**: 從上傳模組接收客戶交易資料
2. **金額處理**: 使用 `lineitem_price` 作為交易金額
3. **資料準備**: 
   - 按客戶彙總資料
   - 按客戶按日期彙總資料
   - 計算 IPT（購買間隔時間）
4. **DNA 分析**: 呼叫 `analysis_dna` 函數
5. **結果處理**: 提取 M 和 F 值
6. **九宮格分類**: 分為低/中/高三個等級
7. **視覺化呈現**: 顯示 Value × Activity 九宮格

## 📊 分析功能

### DNA 分析提供的指標：
- **RFM 分析**: Recency, Frequency, Monetary
- **CAI**: Customer Activity Index（客戶活躍指數）
- **CLV**: Customer Lifetime Value（客戶終身價值）
- **CRI**: Customer Regularity Index（客戶規律性指數）
- **NES**: 客戶狀態分類
- **流失預測**: 基於機器學習的流失機率

### 九宮格區隔：
| 區隔代碼 | 區隔名稱 | 價值 | 活躍度 |
|---------|---------|------|--------|
| A1C | 王者引擎-C | 高 | 高 |
| A2C | 王者穩健-C | 高 | 中 |
| A3C | 王者休眠-C | 高 | 低 |
| B1C | 成長火箭-C | 中 | 高 |
| B2C | 成長常規-C | 中 | 中 |
| B3C | 成長停滯-C | 中 | 低 |
| C1C | 潛力新芽-C | 低 | 高 |
| C2C | 潛力維持-C | 低 | 中 |
| C3C | 清倉邊緣-C | 低 | 低 |

## ✅ 測試結果

- 函數載入：成功
- 資料處理：正常
- DNA 分析：48個指標完整輸出
- 九宮格分類：正確分割，無重複錯誤
- 視覺化：正常顯示

## 🚀 效益

1. **代碼統一性**: l1_basic 和 l2_pro 使用相同的分析邏輯
2. **功能完整性**: 提供完整的 DNA 分析指標
3. **維護便利性**: 只需維護一份 `fn_analysis_dna` 函數
4. **擴展性**: 易於加入新的分析指標
5. **穩定性**: 修正了分位數重複導致的錯誤

## 📌 注意事項

1. 需要確保 `scripts/global_scripts/04_utils/` 目錄下有以下檔案：
   - `fn_analysis_dna.R`
   - `fn_left_join_remove_duplicate2.R`
   - `fn_fct_na_value_to_level.R`

2. 上傳的資料必須包含：
   - `customer_id`: 客戶識別碼
   - `lineitem_price` 或其他金額欄位
   - `payment_time`: 交易時間

3. 如果資料分佈極度不均，系統會自動使用三等分或預設值進行分割

## 🔗 相關檔案

- 主程式：`/app.R`
- 模組檔案：`/modules/module_dna_multi_basic.R`
- DNA 分析函數：`/scripts/global_scripts/04_utils/fn_analysis_dna.R`
- 參考版本：`/l2_pro/TagPilot_pro___v2/modules/module_dna_multi_pro2.R`

## 📅 更新歷史

- 2025-01-03: 初始整合，統一使用 `fn_analysis_dna` 函數
- 2025-01-03: 修正九宮格分割錯誤
- 2025-01-03: 簡化資料處理邏輯，移除 Amazon 特定欄位處理

---

*此文件為 TagPilot 系統的技術更新記錄，供開發團隊參考使用。*